# برومبت Claude Code — تحويل الهوية البصرية للموقع

انسخ ما يلي كاملاً والصقه في Claude Code داخل مجلد مشروع موقعك:

---

أريدك أن تحوّل الهوية البصرية الكاملة لهذا الموقع إلى الهوية البصرية الجديدة الموصوفة أدناه (هوية "دورة القيادة التحويلية"). لا تغيّر أي محتوى نصي أو بنية صفحات أو وظائف — التغيير بصري فقط: ألوان، خطوط، أنماط مكونات، وشعار.

## 1) ابدأ بجرد الوضع الحالي
- افحص بنية المشروع وحدد أين تُعرَّف الأنماط (CSS عادي، Tailwind config، SCSS، styled-components، متغيرات CSS... إلخ).
- استخرج قائمة بالألوان والخطوط الحالية المستخدمة في المشروع قبل أي تعديل، واعرضها عليّ باختصار.
- ضع خطة تنفيذ من خطوات واضحة قبل البدء بالتعديل.

## 2) نظام الألوان الجديد (طبّقه كمتغيرات CSS في `:root` أو في إعدادات Tailwind)

```css
:root{
  --navy:       #14233D; /* كحلي التأسيس — خلفيات كبرى، عناوين مهيبة، أقسام Hero والـfooter الداكنة */
  --navy-mid:   #1F5296; /* درجة وسيطة للتدرجات */
  --blue:       #2A78D6; /* أزرق الثقة — اللون الأساس للعلامة: أزرار رئيسية، روابط، عناصر تفاعلية */
  --blue-track: #CDE2FB; /* أزرق فاتح — خلفيات خفيفة، مسارات تقدّم، عناصر ثانوية على الداكن */
  --gold:       #FAB219; /* ذهبي الأثر — تمييز فقط: خطوط قصيرة، حدود، أيقونات، تفاصيل صغيرة */
  --text-1:     #0B0B0B; /* النصوص الرئيسة */
  --text-2:     #52514E; /* النصوص الفرعية والفقرات */
  --text-3:     #898781; /* التسميات، الحواشي، النصوص الخافتة */
  --bg:         #F9F9F7; /* خلفية الصفحات العامة */
  --surface:    #FCFCFB; /* خلفية البطاقات والأسطح */
  --surface-2:  #F0EFEC; /* أسطح ثانوية، عناصر placeholder */
  --line:       #E1E0D9; /* الحدود والفواصل الخفيفة */
}
```

استبدل كل الألوان القديمة في المشروع بهذه المتغيرات حسب دور كل لون (لا تستبدل hex بـ hex عشوائياً — افهم دور اللون في كل موضع ثم اختر المتغير المناسب).

## 3) قواعد استخدام الألوان (إلزامية)
- **الذهبي #FAB219 للتمييز فقط**: خط سفلي قصير تحت عنوان، حدّ، أيقونة، تفصيل صغير، أو عنصر على خلفية داكنة. **ممنوع**: الذهبي كنص على خلفية فاتحة، أو كخلفية لمساحة كبيرة، أو كلون زر رئيسي بمساحة واسعة.
- **الكحلي #14233D** للعناوين الكبرى والخلفيات المهيبة (Hero، Footer، أقسام مميزة). يمكن استخدام تدرج للأقسام الداكنة: `linear-gradient(135deg, #14233D 0%, #193a67 62%, #1F5296 100%)` مع نص أبيض، ونص ثانوي بلون #D9E4F5، وتسميات خافتة بلون #B9C6DE.
- **الأزرق #2A78D6** هو لون التفاعل: الأزرار الرئيسية، الروابط، حالات hover/focus، الحد السفلي المميز (مثل `border-bottom: 3px solid var(--blue)` في ترويسات الأقسام).
- **تباين ثابت دائماً**: نص داكن على فاتح أو العكس. لا نص متوسط التباين.
- ألوان الحالة (أخضر/برتقالي/أحمر) تُستخدم للحالات التشخيصية فقط (نجاح/تحذير/خطأ) وليست للزخرفة. نجاح: نص #1C6B2D على خلفية #EFF8F0 بحد #BFE3C3. خطأ/تحذير: نص #A5401F على خلفية #FBEFEC بحد #F0C9BE. وسم "معتمد/جديد": نص #0B5D22 على خلفية #DFF3E2 بشكل حبة دواء (border-radius:999px).

## 4) الخطوط
أضف في `<head>`:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+Arabic:wght@400;500;600;700&family=IBM+Plex+Sans:ital,wght@0,400;0,500;0,600;1,400&display=swap" rel="stylesheet">
```
- الخط الأساس لكل الموقع: `font-family:'IBM Plex Sans Arabic','IBM Plex Sans',sans-serif;` مع `line-height:1.7` للنصوص.
- النصوص الإنجليزية/الأرقام/الأكواد داخل المحتوى العربي تُغلّف بصنف:
```css
.en{ font-family:'IBM Plex Sans','IBM Plex Sans Arabic',sans-serif; direction:ltr; unicode-bidi:isolate; }
```
- لا تخلط أي خط آخر مع هذه العائلة في العنوان نفسه. احذف أي استيرادات خطوط قديمة.
- مقياس الطباعة الاسترشادي: h1 ≈ 34px/700 بلون الكحلي، عناوين الأقسام الصغيرة (eyebrow) ≈ 13px/700 بتباعد أحرف `.06em` وأحرف كبيرة بلون --text-3، h3 ≈ 19px/700، الفقرات ≈ 15px بلون --text-2.

## 5) الشعار (علامة الأشكال الأربعة)
استبدل الشعار الحالي بهذه العلامة SVG (أربعة أعمدة متصاعدة بزوايا دائرية — تظهر الأربعة معاً دائماً، بنفس الترتيب والتدرج، ولا تُقطَّع ولا يُعاد ترتيبها):
```html
<svg width="48" height="45" viewBox="0 0 96 90" xmlns="http://www.w3.org/2000/svg" aria-label="القيادة التحويلية">
  <rect x="0"  y="56" width="14" height="24" rx="4" fill="#14233D"/>
  <rect x="24" y="40" width="14" height="40" rx="4" fill="#1F5296"/>
  <rect x="48" y="24" width="14" height="56" rx="4" fill="#2A78D6"/>
  <rect x="72" y="8"  width="14" height="72" rx="4" fill="#FAB219"/>
</svg>
```
- على الخلفيات الداكنة استخدم النسخة الفاتحة: الأشكال الثلاثة الأولى بـ #CDE2FB (بشفافية 0.55 و0.75 للأولين) والثالث #7FB2EA والرابع يبقى ذهبياً #FAB219.
- بجانب العلامة يوضع الاسم النصي بخط 700 بلون الكحلي.
- مساحة عزل: لا يقترب أي عنصر من العلامة بمسافة أقل من ارتفاع أطول أشكالها.

## 6) أنماط المكونات
- **البطاقات**: خلفية `var(--surface)`، حد `1px solid var(--line)`، زوايا `border-radius:14px–16px`، بلا ظلال ثقيلة (الهوية تعتمد الحدود الخفيفة لا الظلال).
- **الأزرار الرئيسية**: خلفية `var(--blue)` بنص أبيض، زوايا دائرية معتدلة. الأزرار الثانوية: حد `1px solid var(--line)` على `var(--surface)`.
- **الوسوم/الشارات**: شكل حبة دواء `border-radius:999px` بحجم 13px وزن 600.
- **ترويسات الأقسام**: نمط العنوان الصغير الخافت بأحرف متباعدة فوق العنوان الرئيسي، ويمكن إضافة الترجمة الإنجليزية كسطر صغير بصنف `.en`.
- **الفواصل والحدود**: دائماً `var(--line)` بسماكة 1px.
- **خط التمييز الذهبي**: عنصر زخرفي قصير `width:52px; height:3px; background:var(--gold); border-radius:2px;` يُستخدم فوق أسماء/تواقيع أو تحت عناوين مختارة.
- **مؤشرات التقدم/الخطوات** (إن وُجدت): مقاطع أفقية `height:8px; border-radius:4px` تتلون بالتدرج كحلي ← #1F5296 ← أزرق للمنجز، و`var(--surface-2)` للمتبقي.

## 7) الاتجاه واللغة
- إن كان الموقع عربياً تأكد من `<html lang="ar" dir="rtl">` واستخدام خصائص منطقية (`padding-inline-start`، `inset-inline-end`، `text-align:start`) بدل left/right الثابتة.

## 8) التنفيذ والتحقق
- طبّق التغييرات ملفاً ملفاً، وابدأ بملف المتغيرات/الثيم المركزي ثم المكونات.
- بعد الانتهاء: افحص كل صفحة وتأكد من عدم بقاء أي لون أو خط من الهوية القديمة (ابحث في الكود عن قيم hex القديمة للتأكد من استبدالها كلها).
- تحقق من تباين النصوص (WCAG AA كحد أدنى) خاصة النصوص على الخلفيات الداكنة.
- تأكد أن الموقع يعمل ويُبنى بلا أخطاء، واعرض عليّ ملخصاً بالملفات المعدّلة وأي مواضع احتجت فيها لاجتهاد.

      # دليل المبرمج — تمرين نظريات القيادة (نسخة الموقع)

**الدورة:** القيادة التحويلية — المحور الأول
**إصدار الحزمة:** 2026-07-22
**الغرض:** تمرينٌ تفاعليٌّ يُعرض على المشارك، يحكم فيه على 15 عبارةً قياديةً (صح/خطأ/محايد) دون أن يعرف نظرية كل عبارة، ثم يختار الأفضل في رأيه، فيظهر تقريرٌ نهائيٌّ كامل.

---

## 1. محتويات الحزمة

| الملف | الوصف |
|---|---|
| `index.html` | الأداة كاملةً، مكتفيةً ذاتياً (HTML + CSS + JS في ملفٍ واحد). تعمل فوراً بلا خادمٍ وبلا مكتبات خارجية. |
| `theories.json` | بيانات التمرين (العبارات، النظريات، المراجع، نصوص التقرير والخلاصة). مرجعٌ منظَّمٌ للتخزين في قاعدة البيانات أو لفصل المحتوى عن العرض. |
| `دليل_المبرمج.md` | هذا الملف. |

> البيانات مُضمَّنةٌ داخل `index.html` (في مصفوفة `THEORIES` وأقسام التقرير) وهي مطابقةٌ لمحتوى `theories.json`. اختر أحد نهجين: إمّا استعمال `index.html` كما هو، أو تغذيته من `theories.json` عبر `fetch` (انظر القسم 5).

---

## 2. التشغيل السريع (drop-in)

الأداة تعمل بمجرد وضع `index.html` في الموقع. طريقتان:

1. **صفحةٌ مستقلة:** ارفع `index.html` كصفحةٍ في مسارٍ خاص (مثل `/tests/leadership-theories/`).
2. **تضمين داخل صفحةٍ قائمة:** ضعها في `iframe`:
   ```html
   <iframe src="/tests/leadership-theories/index.html"
           style="width:100%;height:900px;border:0"></iframe>
   ```

لا حاجة إلى أي إعدادٍ إضافي للتشغيل الأساسي. الحالة كلها في ذاكرة المتصفّح (لا يُستخدم `localStorage`).

---

## 3. تدفّق الاستخدام

```
شاشة المقدمة (اسم اختياري)
      ↓ [ابدأ]
عرض العبارة (بلا اسم نظريتها)  →  اختيار صح/خطأ/محايد  →  انتقال فوري للعبارة التالية
      ↓ (تتكرر 15 مرة)
شاشة اختيار «أفضل نظرية»
      ↓ [اعرض تقريري]
التقرير النهائي (قابل للطباعة / حفظ PDF)
```

**مبدأ أساسي:** لا يُكشف اسم النظرية أثناء التمرين إطلاقاً — الكشف كله يتم في التقرير النهائي فقط.

---

## 4. بنية التقرير النهائي

يتكوّن التقرير من:

1. **ترويسة:** الاسم، تاريخ الإنجاز، رمز عرض التقرير.
2. **لوحة «قراءة في أحكامك»:** رسالةٌ تتبدّل بحسب نمط الإجابات (انظر `report.insight_rules` في `theories.json`).
3. **بنود النتائج (15):** لكل بند: اسم النظرية + المرجع + نصّ العبارة + «إجابتك: صح/خطأ/محايد».
4. **النظرية المفضّلة** التي اختارها المشارك.
5. **الخلاصة والنصائح:** «لا نظريةَ صحيحةً وأخرى خاطئة — بل وجهات نظر» + خمس نصائح (انظر `report.closing`).
6. **تنويه المصدر** (تلخيص كوفي).

الترويسة والتقرير مصمَّمان للطباعة عبر `@media print` (تُخفى عناصر التحكم، ويبقى التقرير نظيفاً). زرّ «طباعة / حفظ PDF» يستدعي `window.print()`.

---

## 5. نقاط الربط بالخادم (اختيارية — لمنصّة الاختبارات)

الأداة تعمل كلياً في المتصفّح. لدمجها في منصّة اختباراتٍ حقيقية (على غرار نظام `print.php` الحالي)، هذه هي نقاط التكامل:

### 5-أ. هوية المستخدم
في `index.html` يوجد حقل اسمٍ اختياري (`#uname`). لربطه بنظام الدخول: احذف الحقل ومرّر الاسم من الخادم، ثم اضبط `rpName` مباشرةً:
```js
document.getElementById('rpName').textContent = SERVER_USER_NAME; // بدل قراءة الحقل
```

### 5-ب. حفظ الإجابات في قاعدة البيانات
عند بلوغ التقرير (`showReport()`)، تتوفّر النتائج في متغيّرات JS:
```js
answers      // مصفوفة من 15 قيمة: "صح" | "خطأ" | "محايد"
bestPick     // فهرس النظرية المفضّلة (0..14) أو null
```
أضِف في نهاية `showReport()` طلب حفظٍ إلى الخادم:
```js
fetch('/api/leadership-theories/submit', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    user: SERVER_USER_ID,
    answers: answers,            // ["صح","خطأ",...] بطول 15
    best: bestPick,              // 0..14 أو null
    finishedAt: new Date().toISOString()
  })
});
```

### 5-ج. رمز عرض التقرير والتاريخ
حالياً يُولَّدان في المتصفّح (`genCode()` و`new Date()`). للمنصّة يُفضّل توليدهما في الخادم وحفظهما، ليُستدعى التقرير لاحقاً برابطٍ ثابت (كما في `print.php?code=...`):
```js
document.getElementById('rpCode').textContent = SERVER_REPORT_CODE;
```

### 5-د. تغذية البيانات من `theories.json` (بدل التضمين)
إن رغبت بفصل المحتوى عن الشيفرة، استبدل مصفوفة `THEORIES` المضمّنة بجلبها من الملف (يعمل على خادمٍ فقط، لا عبر `file://`):
```js
const data = await (await fetch('theories.json')).json();
const THEORIES = data.theories.map(t => ({ s:t.statement, t:t.theory, r:t.reference, n:t.note }));
```
ونصوص المقدمة والخلاصة والنصائح متوفّرة كلها في `theories.json` (أقسام `instructions` و`report.closing`).

---

## 6. التخصيص البصري

- **الألوان:** كلها متغيّرات `CSS` في `:root` أعلى `index.html` (`--brand`, `--brand2`, `--accent`, `--ok`, `--no`, `--mid` ...). عدّلها لتطابق هوية الموقع.
- **الشعار والعنوان:** في `<div class="brandbar">` — استبدل حرف الشعار المؤقّت `ق` بشعار الموقع، وعدّل العنوان.
- **الخطوط:** الخطّ الافتراضي نظام التشغيل؛ يمكن ربط خطّ الموقع.

---

## 7. ملاحظات تقنية

- **RTL:** الصفحة `dir="rtl"` وكل التصميم يدعمه.
- **بلا اعتماديات خارجية:** لا مكتبات، لا خطوط عن بُعد، لا سكربتات `CDN`.
- **بلا تخزين متصفّح:** لا `localStorage` ولا `sessionStorage` (متوافق مع كل البيئات).
- **الأرقام:** لاتينية في كل موضع.
- **متجاوب:** يتكيّف مع الجوّال (الأزرار الثلاثة تتراصّ عمودياً في الشاشات الضيقة).
- **الطباعة:** مضبوطة عبر `@media print`.

---

## 8. تنويه المحتوى

قائمة النظريات الخمس عشرة مصدرها تلخيص ستيفن كوفي في كتابه «العادة الثامنة». صُحِّحت أخطاء نسخٍ إملائية في الأعلام اللاتينية (Collins، Levinson، Kotter). لم تُضَف أي نسبةٍ من خارج نصّ التمرين الأصلي.
